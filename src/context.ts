import React from "react";

const ExchangeStateContext = React.createContext<any>({} as any);
const ExchangeRateContext = React.createContext<any>({} as any);

export { ExchangeStateContext, ExchangeRateContext };
