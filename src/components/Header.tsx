import React from "react";

import "../scss/header.scss";

const Header = () => {
  return (
    <header className="header">
      <ul>
        sdads
      </ul>
    </header>
  );
};

export default Header;
